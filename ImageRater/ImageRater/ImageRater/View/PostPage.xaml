<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ImageRater.View.PostPage">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="StartAndExpand" Padding="10">
            <Label Text="Post" FontSize="40" />
            <StackLayout HorizontalOptions="Center" VerticalOptions="FillAndExpand" Padding="10">
                <Label Text="Please take a photo or upload your own." />
                <Button Text="Take Photo" x:Name="TakePhotoButton" Command="{Binding TakePhotoCommand}" />
                <Button Text="Upload Photo" x:Name="UploadPhotoButton" Command="{Binding UploadPhotoCommand}" />
                <Entry Placeholder="Enter a tag" Text="{Binding CurrentTags}" Margin="10" x:Name="CategoryEntry" />
                <Image HeightRequest="100" Source="{Binding CurrentPhoto}"/>
                <Label Text="{Binding DateTime}" />
                <Label Text="{Binding Location}" />
                <Label Text="{Binding Tags}" />
                <Button Text="Submit" x:Name="SubmitButton" Command="{Binding CreatePostCommand}"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>